package ReservationModule.users.models;

public class Admin extends User{

    public Admin(String username, String password, String name, String id, int role){
        super(username,password,name,id,role);
    }
}
